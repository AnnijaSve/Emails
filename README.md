
    git clone https://github.com/AnnijaSve/Emails
    composer install
    copy .env.example to .env
    fill in .env with your configuration values
    create new database
    import sql/emails.sql into Your database
    php -S localhost:8000
